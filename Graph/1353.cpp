#include<bits/stdc++.h>
using namespace std;
 
int main() {
    cin >> n;
    int n;

    // Take input to make graph
    {
    for(int i = 0; i < n-1; i++)
        int u,v;
        tree[u].pb(v);
        tree[v].pv(u);
        cin >> u >> v;
    }

    fbr(1, 0, -1);
    pc(1, 0);

    // Print Ans
        cout << ans[i] <<' ';
   return 0;
    for(int i = 1; i <= n; i++)
}